src/
├── components/
│ ├── TripForm.tsx ← form inputs, calls API
│ ├── ItineraryDisplay.tsx ← shows day by day results
│ └── DayCard.tsx ← single day plan card
├── services/
│ └── api.ts ← API call logic
├── types.ts ← TypeScript interfaces
├── App.tsx ← holds itinerary state, renders components
└── main.tsx ← entry point, don't need to touch
